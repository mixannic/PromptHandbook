<Window x:Class="PromptHandbook.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="200" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#FFF0F0F0">

    <StackPanel Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Font Family:" 
                      VerticalAlignment="Center" Margin="0,0,10,10"/>
            <ComboBox Grid.Row="0" Grid.Column="1" Name="FontFamilyComboBox" 
                     IsEditable="True" Margin="0,0,0,10" Height="25"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Font Size:" 
                      VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Grid.Row="1" Grid.Column="1" Name="FontSizeComboBox" 
                     IsEditable="True" Height="25"/>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Click="OKButton_Click" Width="70" Height="25" 
                   Margin="0,0,10,0" Background="#FFE1E1E1"/>
            <Button Content="Cancel" Click="CancelButton_Click" Width="70" Height="25"
                   Background="#FFE1E1E1"/>
        </StackPanel>
    </StackPanel>
</Window>